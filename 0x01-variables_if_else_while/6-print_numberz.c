#include <stdio.h>

/**
 * main - Prints numbers.
 *
 * Return:Always 0(success)
 */

int main(void)
{
	 unsigned char f[10] = "0123456789";
	int i;

	for (i = 0; i < 0; i++)
	{
		putchar(f[i]);
	}
	putchar('\n');
	return (0);
}
